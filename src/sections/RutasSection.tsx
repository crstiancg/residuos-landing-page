import React from 'react';
import { RoutesModule } from '../components/RoutesModule';

export const RutasSection: React.FC = () => {
  return (
    <div id="section-rutas" className="animate-in fade-in duration-300">
      <RoutesModule />
    </div>
  );
};
